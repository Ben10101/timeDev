# Experiments

This folder stores exploratory or non-canonical implementations that are useful for reference but are not part of the official Aligna product flow.

Current contents:

- `factory_optimized.py`: archived experiment from the broader software factory phase

The canonical product remains the authenticated application in `frontend/` + `backend/`.
